- page_title _('Issues')

#material-project-issues{ data: { material_issues: true, material_topbar_owner: 'surface.issues', project_id: @project.id, current_user: (current_user ? { id: current_user.id, name: current_user.name, username: current_user.username } : nil).to_json, can_create: can?(current_user, :create_issue, @project), can_update: can?(current_user, :admin_issue, @project), can_delete: can?(current_user, :destroy_issue, @project), new_issue_path: new_project_issue_path(@project), board_path: project_boards_path(@project), work_items_path: project_work_items_path(@project) } }
