# Material operations and security surfaces

The Deploy, Operate, Monitor, Secure, and Security design surfaces now use
host-provided live endpoints. Fixture rows are no longer production defaults;
missing or failed data is reported with an actionable loading/error state.
The exact mount selectors and checked-in design contracts are covered by
`spec/frontend/material_system/ops_security_contract_spec.js`.
